import React, { useEffect, useRef }  from 'react'
import { Container} from './styled';

 
export default function ButtonSecondary({ label, type, icon }: any) {

    return (
        <Container>
            { label }
        </Container>
    );
}